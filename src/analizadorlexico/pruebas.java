
package analizadorlexico;

public class pruebas {
    public static void main(String[] args) {
        String letra = "A";
        
        if (letra.equals(letra.toUpperCase())) {
            System.out.println("es mayus");
        }
        if (letra.equals(letra.toLowerCase())) {
            System.out.println("es minus");
        }
    }
    
}
